export interface EnvironmentVariables {
  NODE_ENV: string;
  PORT: string;
  DB_PORT: string;
  DB_HOST: string;
  DB_NAME: string;
  DB_USERNAME: string;
  DB_PASSWORD: string;
  DB_TYPE: string;
}
